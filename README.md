# markdown-link-checker

Scans markdown files for dead links (remote URLs and local file references) with concurrent checking.

## Quick Start

```bash
./scripts/run.sh README.md
./scripts/run.sh docs/ --format json
```

## Full Documentation

See [SKILL.md](SKILL.md).
